server:
  port: 8888

spring:
  application:
    name: config # 서비스명
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configuration
        prefix: /config
